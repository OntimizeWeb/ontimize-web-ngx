export class OTableExportConfiguration {
  columns: Array<any>;
  columnNames: object;
  sqlTypes: object;
  service: string;
  data?: any[];
  filter?: object;
  mode: string;
  entity: string;
  visibleButtons: string;
  options?: any;
}
